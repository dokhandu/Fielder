class CreatePrimeModel < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :prime_models do |t|
      t.references :modelable, polymorphic: true, index: true
      t.string :name
      t.string :code

      t.timestamps
    end
  end
end
